{
  "name": "@gargoyle/hub",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0"
  }
}
